# mss_dynamic 状态切换优化研究

## 领域与背景
股票量化策略（A股 mss_dynamic V7）
目标：解决市场状态频繁切换导致"今日买明日卖"的问题，提高策略稳定性。

当前问题：
- 最近7个交易日发生4次状态切换
- 部分股票持有不到2天即被卖出
- 交易成本高，持仓周期短

## 修改目标
在 `daily/2026-06-02/cooldown_backtest.py` 中修改三个参数，对比不同设置下的回测绩效。

不要修改其他文件或配置。

## 目标
指标：Calmar Ratio（年化收益 / 最大回撤，越高越好）
方向：最大化
基线（原始策略）：从初步回测结果中获取

## 评估命令
```bash
cd /Users/wangzeshang1/MyProjects/zequant && python3 daily/2026-06-02/cooldown_backtest.py
```
脚本会在 stdout 最后一行输出格式化的评估结果。

## 可调参数

### 方式一：状态切换冷却期
- 参数名: `state_cooldown_days`
- 有效范围: [0, 2, 3, 5, 7]（0=无冷却，2=切换后稳定2天才可再切）
- 影响: 减少频繁切换，但可能错过快速行情转换

### 方式二：bull 状态广度门槛
- 参数名: `breadth_bull_thresh`
- 有效范围: [0.35, 0.40, 0.45, 0.50, 0.55]
- 影响: 提高进入 bull 的难度，减少从 bull 频繁切出的问题

### 方式三：子策略退出最低持有期
- 参数名: `strategy_min_hold_days`
- 有效范围: [0, 3, 5, 7, 10]
- 影响: 状态切换时不立即清仓，给持仓一个锁定期

## 约束条件
- 单次回测必须在 60 秒内完成
- 回测区间: 2019-01-01 到 2026-06-02
- 初始资金: 50,000
- 交易成本: 0.12% 买卖双向
- 不修改原始策略的选股逻辑、止损参数、分配权重
- 只比较综合 Calmar Ratio，兼顾年化收益和最大回撤

## 输出格式
每次实验后，向 `results.tsv` 追加一行：
```
时间戳 \t cooldown_days \t breadth_bull_thresh \t min_hold_days \t annual_return \t max_drawdown \t calmar \t sharpe \t n_switches \t kept|discarded
```
